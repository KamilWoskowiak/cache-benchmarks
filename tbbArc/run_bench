#!/usr/bin/env bash

set -euo pipefail

OUTPUT="tbb_results.csv"
BINARY="./cache_bench_tbb"

THREAD_COUNTS=(
  8
  26
  52
)

MAPPING_SLEEP_US=(
  0
  1
  10
  100
)

OPS=1000000
CACHE_SIZE=10000
DISTRIBUTION="z"
THETA=0.95

# Number of repetitions per configuration.
REPEATS=2

# Working-set sizes to test.
TOTAL_KEYS=(
  10000
  12500
  15000
  20000
  30000
  50000
  75000
)

# Make sure the benchmark binary exists.
if [[ ! -x "$BINARY" ]]; then
  echo "Error: benchmark binary not found or not executable: $BINARY"
  exit 1
fi

# CSV header
echo "repeat,ops_per_thread,threads,cache_size,total_keys,distribution,lambda,theta,warmup,mapping_sleep_us,mapping_spin_us,secs,mops,ns_per_op,warmup_misses" > "$OUTPUT"

for threads in "${THREAD_COUNTS[@]}"; do
  for mapping_sleep_us in "${MAPPING_SLEEP_US[@]}"; do
    for total_keys in "${TOTAL_KEYS[@]}"; do
      echo
      echo "========================================"
      echo "threads=$threads"
      echo "mapping_sleep_us=$mapping_sleep_us"
      echo "total_keys=$total_keys"
      echo "cache_size=$CACHE_SIZE"
      echo "working_set_mib=$(awk "BEGIN { printf \"%.2f\", ($total_keys * 16384) / 1048576 }")"
      echo "========================================"

      for ((repeat=1; repeat<=REPEATS; repeat++)); do
        echo "  run $repeat/$REPEATS"

        result=$(
          "$BINARY" \
            --threads "$threads" \
            --ops "$OPS" \
            --cache_size "$CACHE_SIZE" \
            --total_keys "$total_keys" \
            --distribution "$DISTRIBUTION" \
            --theta "$THETA" \
            --mapping_sleep_us "$mapping_sleep_us" \
            --labeled false \
            2>/dev/null
        )

        echo "$repeat,$result" >> "$OUTPUT"
      done
    done
  done
done

echo
echo "Done."
echo "Results written to: $OUTPUT"
