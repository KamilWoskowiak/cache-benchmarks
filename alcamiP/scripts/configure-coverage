#!/usr/bin/env bash

set -o nounset
set -o errexit
set -o pipefail

this_dir="$(dirname "$(realpath "${0}")")" || exit

source "${this_dir}/.common.rc" || exit

configure_args="-DALCAMI_ENABLE_COVERAGE=1"

# create development build with coverage enabled
"${this_dir}/configure-develop" ${configure_args} "${@}"
